* Add support for dumping SampleValidator to code
* Add support for dumping validator to types
* Add support for exit on first failure
* Optimize union validation on RequiredObject with same name ExactString on all objects
* Implement only allowing known keys
* Look at JS pretty priting of values, fx. [] becomes "", const printedValue = Array.isArray(value) && value.length === 0 ? '[]' : `${value}`
