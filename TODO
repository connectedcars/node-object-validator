* Add support for exit on first failure
* Optimize union validation on RequiredObject with same name ExactString on all objects
* Implement only allowing known keys
* Implement support for aliases:

let objectValidator({
  exact: 'exact_match',
  regexMatch: /my regex match/,
  date: '2021-01-01T15:58:51.099197Z',
  integer: 1,
  float: 1.1
})

* Implement support for sample validators
